<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>
    <nav>
      <ul>
        <% if @current_user == nil %>
          <li>
            <a href="/user_sign_in">Sign In</a>
          </li>
        <% end %>

        <% if @current_user == nil %>
          <li>
            <a href="/user_sign_up">Sign Up</a>
          </li>
        <% end %>
        <% if @current_user != nil %>
          <li>
            <a href="/user_sign_out">Sign Out</a>
          </li>
          <li>
            <a href="/users/<%= @current_user.username %>">My Profile</a>
          </li>
          <li>
            <a href="/search_users">Search Users</a>
          </li>
          <li>
            <a href="/cancel_user_account">Cancel Account</a>
          </li>
        <% end %>
      </ul>
    </nav>

    <% if notice.present? %> 
      <div style="color: green">
        <%= notice %>
      </div>
    <% end %>

    <% if alert.present? %>
      <div style="color: red">
        <%= alert %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>

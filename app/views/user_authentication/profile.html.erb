<h1><%= @the_user.username %></h1>

<% if @the_user.id == @current_user.id %>
  <h3> My Friends (<%= @pending_requests %> pending)</h3>
<% else %>
  <% if @no_connection %>
      <form action="/insert_friend_request" method="post">
        <input id="recipient_id" name="query_recipient_id" value=<%= @the_user.id %> type="hidden">
        <button>
          Add <%= @the_user.username %> as a friend
        </button>
      </form>
  <% elsif @the_friend_request_sent != nil and @the_friend_request_sent.status == "Pending" %>
    <dl>Friend request pending</dl>
  <% elsif @the_friend_request_received != nil and @the_friend_request_received.status == "Pending" %>
    <dl>Accept friend request</dl>
  <% end %>
  <h3> Friends </h3>
<% end %>


<hr>
<% if @the_user.id == @current_user.id %>
  <h2>My Albums</h2>
  <dl>Create New Album</dl>

  <table border="1">
    <tr>
      <th>
        Image
      </th>

      <th>
        Name
      </th>

      <th>
      </th>
    </tr>

    <% @list_of_my_albums.each do |an_album| %>
    <tr>

      <td>
        <img src="<%= an_album.image %>">
      </td>

      <td>
        <%= an_album.name %>
      </td>

      <td>
        <a href="/albums/<%= an_album.id %>">
          Go to album
        </a>
      </td>
    </tr>
    <% end %>
  </table>
<% end %>

<% if @the_user.id != @current_user.id and @are_friends %>
  <h2>All Albums</h2>

  <table border="1">
    <tr>
      <th>
        Image
      </th>

      <th>
        Name
      </th>

      <th>
      </th>
    </tr>

    <% @current_user.albums.each do |an_album| %>
    <tr>

      <td>
        <img src="<%= an_album.image %>">
      </td>

      <td>
        <%= an_album.name %>
      </td>

      <td>
        <a href="/albums/<%= an_album.id %>">
          Go to album
        </a>
      </td>
    </tr>
    <% end %>
  </table>
<% else %>
  <dl>Must be friends to see <%= @the_user.username %>'s albums</dl>
<% end %>
